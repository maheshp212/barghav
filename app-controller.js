'use strict';

appModule.controller('mainAppCtrlssss', function($scope, $location) {
        
    // $scope.burl = $location.protocol() + '://' + $location.host() + ':' + $location.port();
    $location.path('/greesssn');
});